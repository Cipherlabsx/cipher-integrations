{
  "name": "cipher-integrations",
  "version": "0.1.0",
  "private": true,
  "description": "CipherDLMM integration connectors — Jupiter, DefiLlama, GeckoTerminal, DexScreener, Birdeye",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
